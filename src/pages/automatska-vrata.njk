---
layout: layouts/base.njk
permalink: /proizvodi/automatska-vrata/
title: "SMIŽ - Automatska vrata"
description: "SMIŽ - Automatska vrata GEZE. Pouzdana klizna i krilna rešenja za objekte sa visokim protokom ljudi i zahtevima za komfor i bezbednost."
ogTitle: "SMIŽ - Automatska vrata"
ogDescription: "Automatska klizna i krilna vrata GEZE za bolnice, poslovne i javne objekte. Precizna ugradnja i stručan servis."
ogImage: "/images/logo-mini.png"
pageType: product
isProductPage: true
---

{# --- HERO SEKCIJA --- #}
<section class="product-hero">
    <div class="hero-bg">
        <img src="/images/door/AKV staklena.jpeg" alt="Automatska vrata" class="hero-img">
        <div class="hero-overlay"></div>
    </div>
    <div class="container">
        <div class="hero-content">
            <nav class="breadcrumb">
                <a href="/">Početna</a> / <a href="/#proizvodi">Proizvodi</a>
            </nav>
            <h1>Automatska <span class="text-accent">vrata</span></h1>
            <p class="hero-lead">Vrhunska GEZE tehnologija za maksimalnu frekvenciju prolaza i beskompromisnu bezbednost.</p>
        </div>
    </div>
</section>

{# --- UVOD I PRIMENA --- #}
<section class="section">
    <div class="container">
        <div class="product-intro-grid">
            <div class="intro-main">
                <span class="badge">GEZE Partner</span>
                <h2>Preciznost i pouzdanost u svakom pokretu</h2>
                <p>Automatska vrata su srce svakog modernog objekta sa velikim protokom ljudi. SMIŽ nudi kompletna rešenja bazirana na **GEZE pogonskim sistemima**, koji su sinonim za tih rad, dugovečnost i energetsku efikasnost.</p>
                <p>Bilo da je reš o luksuznom hotelu, bolnici ili frekventnom tržnom centru, naši sistemi se prilagođavaju arhitekturi i specifičnim zahtevima sigurnosti.</p>
            </div>
            <div class="intro-side">
                <div class="usage-box">
                    <h3>Idealno za:</h3>
                    <ul>
                        <li>Bolnice i laboratorije</li>
                        <li>Poslovne i javne objekte</li>
                        <li>Hotele i restorane</li>
                        <li>Trgovinske centre</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>

{# --- GLAVNE KATEGORIJE --- #}
<section class="section-alt">
    <div class="container">
        <div class="variants-layout">
            
            {# KLIZNA VRATA #}
            <div class="variant-block">
                <div class="variant-header">
                    <span class="variant-num">01</span>
                    <h2>Automatska klizna vrata</h2>
                </div>
                <p>Elegantna rešenja koja štede prostor i pružaju maksimalnu širinu prolaza. Savršena za glavne ulaze.</p>
                <div class="spec-grid">
                    <div class="spec-item">
                        <h4>Sistemi</h4>
                        <p>GEZE EC Drive, Slimdrive, Powerdrive</p>
                    </div>
                    <div class="spec-item">
                        <h4>Tipovi krila</h4>
                        <p>Staklena (uramljena/bez rama), Teleskopska, Drvena</p>
                    </div>
                </div>
            </div>

            {# KRILNA VRATA #}
            <div class="variant-block">
                <div class="variant-header">
                    <span class="variant-num">02</span>
                    <h2>Automatska zaokretna vrata</h2>
                </div>
                <p>Idealna za unutrašnje komunikacije ili ulaze gde klizni sistemi nisu mogući. Visoka kontrola pristupa.</p>
                <div class="spec-grid">
                    <div class="spec-item">
                        <h4>Sistemi</h4>
                        <p>EC Turn, Slimdrive EMD, Powerturn</p>
                    </div>
                    <div class="spec-item">
                        <h4>Konfiguracija</h4>
                        <p>Jednokrilna i dvokrilna rešenja sa metalnim ili drvenim štokom</p>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

{# --- CTA --- #}
<section class="section cta-minimal">
    <div class="container">
        <div class="cta-flex">
            <h2>Projektujemo rešenje po vašoj meri</h2>
            <a href="/kontakt/" class="btn btn-primary">Zatražite ponudu</a>
        </div>
    </div>
</section>

<style>
    :root {
        --accent: #005e83;
        --dark: #1a1a1a;
        --light: #f4f4f4;
        --white: #ffffff;
        --transition: all 0.3s ease;
    }

    .text-accent { color: var(--accent); }
    .badge { 
        display: inline-block; 
        padding: 4px 12px; 
        background: rgba(0, 94, 131, 0.12); 
        color: var(--accent); 
        font-weight: 700; 
        font-size: 0.75rem; 
        text-transform: uppercase; 
        margin-bottom: 1rem;
    }

    /* Hero */
    .product-hero {
        height: 50vh;
        min-height: 400px;
        position: relative;
        display: flex;
        align-items: center;
        color: var(--white);
    }
    .hero-bg { position: absolute; inset: 0; z-index: -1; }
    .hero-img { width: 100%; height: 100%; object-fit: cover; }
    .hero-overlay { position: absolute; inset: 0; background: linear-gradient(to top, rgba(0,0,0,0.8), rgba(0,0,0,0.3)); }
    
    .breadcrumb { font-size: 0.8rem; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 1rem; opacity: 0.7; }
    .breadcrumb a { color: var(--white); text-decoration: none; }
    .product-hero h1 { font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 900; margin-bottom: 1rem; }
    .hero-lead { font-size: 1.2rem; max-width: 600px; opacity: 0.9; }

    /* Intro Grid */
    .product-intro-grid {
        display: grid;
        grid-template-columns: 1.5fr 1fr;
        gap: 4rem;
        align-items: flex-start;
    }
    .intro-main h2 { font-size: 2.2rem; font-weight: 800; margin-bottom: 1.5rem; line-height: 1.2; }
    .intro-main p { font-size: 1.1rem; line-height: 1.7; color: #444; margin-bottom: 1.2rem; }

    .usage-box {
        background: var(--dark);
        color: var(--white);
        padding: 2.5rem;
        border-radius: 2px;
    }
    .usage-box h3 { font-size: 1.2rem; margin-bottom: 1.5rem; border-bottom: 1px solid #444; padding-bottom: 0.5rem; }
    .usage-box ul { list-style: none; padding: 0; }
    .usage-box li { margin-bottom: 0.8rem; font-weight: 500; display: flex; align-items: center; }
    .usage-box li::before { content: "→"; color: var(--accent); margin-right: 10px; }

    /* Variants Layout */
    .variants-layout {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
    }
    .variant-block {
        background: var(--white);
        padding: 3rem;
        border: 1px solid #eee;
        transition: var(--transition);
    }
    .variant-block:hover { border-color: var(--accent); box-shadow: 0 10px 30px rgba(0,0,0,0.05); }
    
    .variant-header { display: flex; align-items: center; gap: 1.5rem; margin-bottom: 1.5rem; }
    .variant-num { font-size: 2.5rem; font-weight: 900; color: #f0f0f0; }
    .variant-block h2 { font-size: 1.6rem; font-weight: 800; }
    .variant-block p { color: #666; margin-bottom: 2rem; }

    .spec-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.5rem;
        border-top: 1px solid #eee;
        padding-top: 1.5rem;
    }
    .spec-item h4 { font-size: 0.85rem; text-transform: uppercase; color: var(--accent); letter-spacing: 1px; margin-bottom: 0.3rem; }
    .spec-item p { margin-bottom: 0; color: var(--dark); font-weight: 600; }

    /* CTA Minimal */
    .cta-flex {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: var(--light);
        padding: 3rem;
        gap: 2rem;
    }
    .cta-flex h2 { font-size: 1.8rem; font-weight: 800; margin: 0; }

    /* Utilities */
    .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
    .section { padding: 80px 0; }
    .section-alt { padding: 80px 0; background: #fafafa; }
    .btn {
        display: inline-block;
        padding: 1.1rem 2.2rem;
        background: var(--accent);
        color: var(--white);
        text-decoration: none;
        font-weight: 700;
        text-transform: uppercase;
        transition: var(--transition);
    }
    .btn:hover { background: var(--dark); transform: translateY(-2px); }

    @media (max-width: 992px) {
        .product-intro-grid, .variants-layout { grid-template-columns: 1fr; }
        .cta-flex { flex-direction: column; text-align: center; }
    }
</style>